# Dữ liệu

Đặt dữ liệu DeepPCB gốc trong `data/raw/` (hoặc ở bất kỳ nơi nào ngoài repo), rồi dùng
`scripts/prepare_deeppcb.py` để tạo dữ liệu YOLO trong `data/processed/`. Hai thư mục dữ
liệu bị Git bỏ qua; chỉ `.gitkeep` được lưu.

